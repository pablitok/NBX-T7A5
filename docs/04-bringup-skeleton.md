# 04 - Initial bring-up skeleton

This repository includes a **non-buildable skeleton** only, based on verified facts and explicit TODOs.

- `device/noblex/nbx_t7a5/BoardConfig.mk` includes placeholders for unknown addresses/sizes.
- `device/noblex/nbx_t7a5/device.mk` and product makefiles are scaffolds.
- `vendor/noblex/nbx_t7a5/` contains placeholders only; no proprietary binaries are committed.
- `kernel/noblex/sofia3gr/README.md` documents required kernel artifacts.

Do not replace TODO placeholders with guesses.
